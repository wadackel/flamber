import { Router } from "express";

const router = Router();

router.get("/", (req, res) => {
  res.errorJSON("TODO");
});

router.post("/", (req, res) => {
  res.errorJSON("TODO");
});

router.put("/", (req, res) => {
  res.errorJSON("TODO");
});

router.delete("/", (req, res) => {
  res.errorJSON("TODO");
});

export default router;

