Basic:

```
<div>
  <EmptyData
    icon={<BoardIcon />}
    title="No boards"
    action={<RaisedButton>Add board</RaisedButton>}
  >
    There is no board.<br />
    Let's add a new board.
  </EmptyData>

  <EmptyData
    size="sm"
    icon={<BoardIcon />}
    title="No boards"
  >
    There is no board.<br />
    Let's add a new board.
  </EmptyData>
</div>
```
